[Unit]
Description=BOLT debug station daemon (raw RXSM stream + HTTP API)
After=network.target
StartLimitIntervalSec=0

[Service]
ExecStart=/usr/local/bin/bolt-station --lo-idle low
Restart=always
RestartSec=2
StateDirectory=bolt-station

[Install]
WantedBy=multi-user.target
